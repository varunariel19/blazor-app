@using System.Security.Claims
@using SolveIt.Services
@using SolveIt.Models
@using SolveIt.Components.Modules
@using Microsoft.AspNetCore.Identity
@using Microsoft.AspNetCore.Components.Authorization
@inject SolveIt.Services.UserStateService UserState
@inject AuthenticationStateProvider AuthProvider
@inject UserManager<User> UserManager
@inject SolveIt.UI_state.UiStateService UiState
@inherits LayoutComponentBase
@implements IDisposable

<CascadingValue Value="UserState">
    <div class="app-wrapper">

        <nav class="navbar">
            <div class="navbar-brand">
                <span class="brand-icon">⬡</span>
                <span class="brand-name">Solve<span class="brand-accent">It</span></span>
            </div>

            <div class="navbar-search">
                <div class="search-container">
                    <svg class="search-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <circle cx="11" cy="11" r="8" />
                        <path d="m21 21-4.35-4.35" />
                    </svg>
                    <input type="text" placeholder="Search questions, tags, users..." class="search-input" />
                    <kbd class="search-kbd">⌘K</kbd>
                </div>
            </div>

            <div class="navbar-actions">
                <button class="nav-btn ask-btn" @onclick="UiState.ToggleAskForm">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
                        <path d="M12 5v14M5 12h14" />
                    </svg>
                    Ask Question
                </button>

                <button class="nav-icon-btn" title="Notifications">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9" />
                        <path d="M13.73 21a2 2 0 0 1-3.46 0" />
                    </svg>
                    <span class="notif-badge">3</span>
                </button>

                <button class="nav-icon-btn" title="Inbox">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z" />
                        <polyline points="22,6 12,13 2,6" />
                    </svg>
                </button>

                @if (UserState.IsAuthenticated)
                {
                    <UserMenu />
                }
                else
                {
                    <a href="/login">Sign in</a>
                }
            </div>
        </nav>

        <div class="content-wrapper">
            <aside class="sidebar">
                <div class="sidebar-section">
                    <p class="sidebar-label">NAVIGATE</p>
                    <nav class="sidebar-nav">
                        <a href="/" class="sidebar-link active">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z" />
                                <polyline points="9 22 9 12 15 12 15 22" />
                            </svg>
                            Home
                        </a>
                        <a href="/questions" class="sidebar-link">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <circle cx="12" cy="12" r="10" />
                                <path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3" />
                                <path d="M12 17h.01" />
                            </svg>
                            Questions
                            <span class="sidebar-count">1.2k</span>
                        </a>
                        <a href="/tags" class="sidebar-link">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M20.59 13.41l-7.17 7.17a2 2 0 0 1-2.83 0L2 12V2h10l8.59 8.59a2 2 0 0 1 0 2.82z" />
                                <line x1="7" y1="7" x2="7.01" y2="7" />
                            </svg>
                            Tags
                        </a>
                        <a href="/users" class="sidebar-link">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
                                <circle cx="9" cy="7" r="4" />
                                <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
                                <path d="M16 3.13a4 4 0 0 1 0 7.75" />
                            </svg>
                            Users
                        </a>
                    </nav>
                </div>

                <div class="sidebar-section">
                    <p class="sidebar-label">MY STUFF</p>
                    <nav class="sidebar-nav">
                        <a href="/profile" class="sidebar-link">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2" />
                                <circle cx="12" cy="7" r="4" />
                            </svg>
                            Profile
                        </a>
                        <a href="/saved" class="sidebar-link">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="m19 21-7-4-7 4V5a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2v16z" />
                            </svg>
                            Saved
                            <span class="sidebar-count">7</span>
                        </a>
                    </nav>
                </div>

                <div class="sidebar-section">
                    <p class="sidebar-label">TOP TAGS</p>
                    <div class="tag-list">
                        <span class="tag">blazor</span>
                        <span class="tag">c#</span>
                        <span class="tag">.net</span>
                        <span class="tag">javascript</span>
                        <span class="tag">css</span>
                        <span class="tag">sql</span>
                        <span class="tag">azure</span>
                        <span class="tag">api</span>
                    </div>
                </div>
            </aside>

            <main class="main-content">
                @Body
            </main>

            <aside class="right-panel">
                <div class="widget">
                    <h4 class="widget-title">🔥 Hot Today</h4>
                    <ul class="hot-list">
                        <li><a href="#">How to inject services in Blazor WASM?</a></li>
                        <li><a href="#">Difference between Task and ValueTask?</a></li>
                        <li><a href="#">EF Core vs Dapper performance benchmark</a></li>
                        <li><a href="#">Best practices for JWT in .NET 8</a></li>
                        <li><a href="#">Async void vs async Task in event handlers</a></li>
                    </ul>
                </div>

                <div class="widget">
                    <h4 class="widget-title">🏆 Top Contributors</h4>
                    <div class="contributor-list">
                        @for (int i = 1; i <= 5; i++)
                        {
                            var idx = i;
                            <div class="contributor">
                                <img src="@($"https://api.dicebear.com/7.x/shapes/svg?seed=user{idx * 7}")" alt="user" />
                                <div>
                                    <p class="contributor-name">dev_user_@idx</p>
                                    <p class="contributor-rep">@(5280 - idx * 320) rep</p>
                                </div>
                                <span class="rank-badge">#@idx</span>
                            </div>
                        }
                    </div>
                </div>
            </aside>
        </div>
    </div>
</CascadingValue>

@code {
    protected override void OnInitialized()
    {
        UiState.OnChange += HandleChange;
    }

    private async void HandleChange() => await InvokeAsync(StateHasChanged);

    public void Dispose()
    {
        UiState.OnChange -= HandleChange;
    }

    protected override async Task OnInitializedAsync()
    {
        var authState = await AuthProvider.GetAuthenticationStateAsync();
        var claimsUser = authState.User;

        if (claimsUser.Identity?.IsAuthenticated == true)
        {
            var id = claimsUser.FindFirst(ClaimTypes.NameIdentifier)?.Value;

            if (id != null)
            {
                var dbUser = await UserManager.FindByIdAsync(id);

                if (dbUser != null)
                {
                    UserState.SetUser(new UserDto
                        {
                            Id = id,
                            Email = dbUser.Email ?? "",
                            DisplayName = dbUser.DisplayName ?? dbUser.Email?.Split('@')[0] ?? "User",
                            AvatarUrl = dbUser.AvatarUrl ?? ""
                        });
                }
            }
        }
    }
}